# Build settings
[build]
  command = "npm run build"
  publish = "build"
  
# Redirect all requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variables (set these in Netlify dashboard)
# REACT_APP_API_URL will be your backend URL after deployment
