# Frontend Environment Variables for Production
# Copy this file to .env.local and fill in your production values

# Backend API URL (required for production)
# For Netlify: Set this in Netlify dashboard under Site settings → Environment variables
# Example: https://your-backend.onrender.com
REACT_APP_API_URL=http://localhost:5001

# Note: All React environment variables MUST start with REACT_APP_
# They are embedded into the build at build time
